apiVersion: v1
kind: Service
metadata:
  namespace: exercises
  name: ping-pong
spec:
  type: NodePort
  selector:
    app: ping-pong
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
